<div>
  <h2>{{.description}}</h2>
  <h3>{{.contracts}}</h3>
</div>
<div class="mt-5">
  <button 
    hx-get="round/{{.nextRound}}"
    hx-target="#game"
    class="bg-red-500 text-7xl hover:bg-blue-700 text-white font-bold py-4 px-8 rounded xl:bg-blue-500 xl:text-lg xl:py-2 xl:px-4"
  >
    Next Round {{.nextRound}}
  </button>
</div>
